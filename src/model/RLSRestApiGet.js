/*
 * Superset
 * Superset
 *
 * OpenAPI spec version: v1
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.44
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';
import {Roles1} from './Roles1';
import {Tables} from './Tables';

/**
 * The RLSRestApiGet model module.
 * @module model/RLSRestApiGet
 * @version v1
 */
export class RLSRestApiGet {
  /**
   * Constructs a new <code>RLSRestApiGet</code>.
   * @alias module:model/RLSRestApiGet
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>RLSRestApiGet</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/RLSRestApiGet} obj Optional instance to populate.
   * @return {module:model/RLSRestApiGet} The populated <code>RLSRestApiGet</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new RLSRestApiGet();
      if (data.hasOwnProperty('clause'))
        obj.clause = ApiClient.convertToType(data['clause'], 'String');
      if (data.hasOwnProperty('description'))
        obj.description = ApiClient.convertToType(data['description'], 'String');
      if (data.hasOwnProperty('filter_type'))
        obj.filterType = ApiClient.convertToType(data['filter_type'], 'String');
      if (data.hasOwnProperty('group_key'))
        obj.groupKey = ApiClient.convertToType(data['group_key'], 'String');
      if (data.hasOwnProperty('id'))
        obj.id = ApiClient.convertToType(data['id'], 'Number');
      if (data.hasOwnProperty('name'))
        obj.name = ApiClient.convertToType(data['name'], 'String');
      if (data.hasOwnProperty('roles'))
        obj.roles = ApiClient.convertToType(data['roles'], [Roles1]);
      if (data.hasOwnProperty('tables'))
        obj.tables = ApiClient.convertToType(data['tables'], [Tables]);
    }
    return obj;
  }
}

/**
 * This is the condition that will be added to the WHERE clause. For example, to only return rows for a particular client, you might define a regular filter with the clause `client_id = 9`. To display no rows unless a user belongs to a RLS filter role, a base filter can be created with the clause `1 = 0` (always false).
 * @member {String} clause
 */
RLSRestApiGet.prototype.clause = undefined;

/**
 * Detailed description
 * @member {String} description
 */
RLSRestApiGet.prototype.description = undefined;

/**
 * Allowed values for the <code>filterType</code> property.
 * @enum {String}
 * @readonly
 */
RLSRestApiGet.FilterTypeEnum = {
  /**
   * value: "Regular"
   * @const
   */
  regular: "Regular",

  /**
   * value: "Base"
   * @const
   */
  base: "Base"
};
/**
 * Regular filters add where clauses to queries if a user belongs to a role referenced in the filter, base filters apply filters to all queries except the roles defined in the filter, and can be used to define what users can see if no RLS filters within a filter group apply to them.
 * @member {module:model/RLSRestApiGet.FilterTypeEnum} filterType
 */
RLSRestApiGet.prototype.filterType = undefined;

/**
 * Filters with the same group key will be ORed together within the group, while different filter groups will be ANDed together. Undefined group keys are treated as unique groups, i.e. are not grouped together. For example, if a table has three filters, of which two are for departments Finance and Marketing (group key = 'department'), and one refers to the region Europe (group key = 'region'), the filter clause would apply the filter (department = 'Finance' OR department = 'Marketing') AND (region = 'Europe').
 * @member {String} groupKey
 */
RLSRestApiGet.prototype.groupKey = undefined;

/**
 * Unique if of rls filter
 * @member {Number} id
 */
RLSRestApiGet.prototype.id = undefined;

/**
 * Name of rls filter
 * @member {String} name
 */
RLSRestApiGet.prototype.name = undefined;

/**
 * @member {Array.<module:model/Roles1>} roles
 */
RLSRestApiGet.prototype.roles = undefined;

/**
 * @member {Array.<module:model/Tables>} tables
 */
RLSRestApiGet.prototype.tables = undefined;

